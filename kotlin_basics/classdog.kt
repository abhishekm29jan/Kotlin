package com.example.kotlin_basics

fun main() {
    // Creating an object/ instance of a class
    var mydog = Dog("Jack", "German Shepherd", 3)
    //println("${mydog.name} is a ${mydog.breed} and ${mydog.age} yrs old")
}